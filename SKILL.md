---
name: cn-ecommerce-ops
description: 国内电商全链路运营专家技能，覆盖淘宝、天猫、京东、拼多多、抖音电商、小红书、视频号六大平台。提供选品评分、定价与利润测算、广告投放ROI测算、店铺转化漏斗诊断、广告法违禁词合规检查五个可执行工具，以及平台流量机制、选品方法论、标题主图详情页优化、直播短视频、运营节奏与风控的完整知识库。当用户涉及国内电商选品、定价、算利润、算ROI、投直通车或千川、做店铺诊断、优化标题主图详情页、写商品文案、直播运营、检查文案是否违反广告法、平台规则与流量玩法等问题时，应使用本技能。
license: MIT
agent_created: true
---

# 国内电商全链路运营

## 概述

本技能把国内电商运营中最依赖经验判断的环节，转化为可计算、可复核的
决策流程。核心主张是：**先算账，再做事**。

多数电商决策失误源于三件事——不算真实利润（漏算退货与推广成本）、
不算保本 ROI（盲目投放）、不查合规（广告法罚则起点二十万元）。
本技能为这三件事各提供了确定性工具。

## 何时使用

在遇到以下任一场景时使用本技能：

- 判断某款产品值不值得做（选品评估、竞品分析）
- 定价、核算真实利润、计算保本售价
- 投放前测算保本 ROI、可承受最高出价、目标 ROI 反推
- 店铺流量正常但不出单、转化率下滑、ROI 做不上去
- 撰写或审核商品标题、主图文案、详情页、短视频脚本、直播话术
- 检查文案是否触碰广告法违禁词与极限词
- 了解淘宝/天猫/京东/拼多多/抖音/小红书的流量机制与冷启动打法
- 制定日常运营节奏、大促排期、备货计划

## 快速路由

根据用户问题类型，选择对应工具或文档：

| 用户问题类型 | 使用 |
|------------|------|
| 这个品能不能做 | `scripts/product_score.py` + `references/product-selection.md` |
| 卖多少钱／能赚多少／保本价 | `scripts/pricing.py` |
| 广告怎么投／ROI 多少才不亏／出价多少 | `scripts/ad_calc.py` |
| 有流量不出单／哪个环节有问题 | `scripts/diagnose.py` |
| 文案能不能这么写／会不会违规 | `scripts/compliance.py` |
| 平台怎么起量／算法逻辑 | `references/platform-playbook.md` |
| 标题主图详情页／短视频／直播 | `references/listing-and-content.md` |
| 日常怎么做／大促怎么排／怎么防违规 | `references/operations-playbook.md` |

## 可执行工具

全部脚本使用 Python 3 标准库，无第三方依赖，可直接运行。
所有脚本均支持 `--json` 输出结构化结果，支持 `--self-test` 自检。
运行前先切换到 `scripts/` 目录，或确保该目录在 `PYTHONPATH` 中。

### 1. 定价与利润测算 —— pricing.py

采用 100 单基准法核算，把退货造成的去程运费、包材、损耗全部计入，
这是"卖爆了却在亏钱"的根本原因。

```bash
python scripts/pricing.py --cost 35 --price 129 --platform douyin \
    --shipping 4.5 --packaging 1.5 --return-rate 30 --ad-ratio 18
```

输出单均净利、净利率、毛利率、**保本售价**、**保本 ROI** 与成本结构。
`--list-platforms` 查看内置平台费率参考表。

**关键用法**：在任何定价、报价、谈达人佣金、参加活动前先跑一次。
得到的"保本售价"是价格底线，"保本 ROI"是投放的生死线。

### 2. 投放测算 —— ad_calc.py

适用于直通车、万相台、巨量千川、京准通、多多推广等所有 CPC 场景。

```bash
python scripts/ad_calc.py --price 129 --gross-margin 60 --cvr 2.5 --cpc 1.2 \
    --budget 2000 --target-roi 4
```

输出 UV 价值、当前 ROI、保本 ROI、**可承受最高 CPC**、CPA、
日预算测算，以及达成目标 ROI 所需的 CPC 或转化率（二选一路径）。

**核心恒等式**：`ROI = UV价值 ÷ CPC`，`保本ROI = 1 ÷ 毛利率`。
毛利率参数应使用 `pricing.py` 算出的毛利率，两个工具串联使用。

### 3. 转化漏斗诊断 —— diagnose.py

不看哪个数字低，而是看**哪个环节相对行业基准差得最多**，
偏离最大的环节即为瓶颈，优先修复它的投入产出比最高。

```bash
python scripts/diagnose.py --impression 200000 --click 2000 --cart 200 \
    --order 110 --paid 55 --refund 8 --gmv 6600
```

输出各环节转化率与基准对比、瓶颈定位、按优先级排序的处方，
以及"瓶颈修复到基准后订单能涨多少"的提升模拟。

**使用原则**：转化链路存在短板时加预算等于放大亏损，先诊断再投放。

### 4. 选品六维评分 —— product_score.py

利润空间 25%、市场需求 20%、竞争强度 20%、退货风险 15%、
物流成本 10%、供应链 10%，加权总分 100。

```bash
python scripts/product_score.py --gross-margin 62 --search-index 8000 \
    --trend up --supply-ratio 3.2 --return-rate 18 --weight 0.4 \
    --moq 100 --restock-days 10
```

输出总分、各维度得分、短板识别与改进方向。
阈值：≥75 优质款，60–74 可做款小批量测试，<60 建议放弃。

### 5. 合规检查 —— compliance.py

内置 175 条广告法违禁词库，分三级风险：
P0 明令禁止（绝对化用语，命中即违法）、P1 需要资质、P2 真实性约束。

```bash
python scripts/compliance.py --text "全网最低价，销量第一"
python scripts/compliance.py --file detail.txt --min-level P0
```

输出命中词、风险级别、原文上下文与整改建议。
**命中 P0 时进程返回码为 1**，可用于批处理或 CI 流程拦截。

**强制要求**：所有对外文案（标题、主图文字、详情页、短视频脚本、
直播话术、客服话术）发布前必须扫描，P0 必须清零。

## 知识库

需要方法论、平台规则或实操细节时，按需读取以下文档：

- **`references/platform-playbook.md`** —— 六大平台流量机制对比、
  淘宝搜索权重与坑产逻辑、拼多多价格力与活动起量、抖音赛马机制与 GPM、
  京东与小红书打法、跨平台通用铁律。
- **`references/product-selection.md`** —— 选品四道门槛（合规/利润/
  需求/竞争）、评分模型说明、分品类退货率参考、四种产品角色矩阵、
  供应链评估与备货公式、六大选品陷阱。
- **`references/listing-and-content.md`** —— 各平台标题字数与结构公式、
  主图五图法则、详情页九屏黄金结构、短视频脚本框架、直播节奏与货盘、
  评价运营、发布前检查清单。
- **`references/operations-playbook.md`** —— GMV 公式拆解、核心指标
  定义与健康区间、日周月运营节奏、提升客单价六法、大促五阶段节奏、
  售后与差评处理、平台与法律风控红线、常见问题速查。

## 典型工作流

### 工作流 A：评估一个新品能不能做

1. 用 `product_score.py` 做六维评分，得到初步结论
2. 若 ≥60 分，用 `pricing.py` 精算利润，确认保本售价与毛利率
3. 用 `ad_calc.py` 代入毛利率，算出保本 ROI 与最高可承受 CPC
4. 判断该 CPC 在目标类目是否可实现（对比行业平均 CPC）
5. 读 `references/product-selection.md` 核对合规资质与供应链风险
6. 输出结论：做 / 不做 / 小批量测试，并给出价格与投放边界

### 工作流 B：店铺有流量但不出单

1. 用 `diagnose.py` 输入完整漏斗数据，定位瓶颈环节
2. 按输出的处方逐条执行，不要跳过优先级
3. 若瓶颈在点击率 → 读 `listing-and-content.md` 主图章节
4. 若瓶颈在转化率 → 读详情页九屏结构与评价运营章节
5. 若瓶颈在支付率 → 检查运费门槛、优惠券、库存与催付
6. 修复后重新跑 `diagnose.py` 对比验证

### 工作流 C：投放亏损，ROI 做不上去

1. 用 `pricing.py` 确认真实毛利率（多数人此处高估）
2. 用 `ad_calc.py` 算保本 ROI，与实际 ROI 对比
3. 若实际 ROI < 保本 ROI，用 `--target-roi` 反推需要的 CPC 或转化率
4. 优先级固定为：提转化率 > 降 CPC > 提客单价
5. 转化率问题回到工作流 B 做漏斗诊断

### 工作流 D：撰写或审核商品文案

1. 读 `references/listing-and-content.md` 对应章节获取结构模板
2. 按平台字数限制与结构公式撰写
3. 用 `compliance.py` 扫描，P0 必须清零，P1 确认资质，P2 确认可举证
4. 对照文档末尾的发布前检查清单逐项确认

## 铁律

1. **先算账，再做事。** 定价、投放、报活动、谈佣金之前，
   必须先跑 `pricing.py` 和 `ad_calc.py`，没有保本售价与保本 ROI
   这两个数字就是在盲目决策。
2. **诊断优先于加预算。** 转化链路有短板时加预算只会放大亏损。
3. **合规先于增长。** 广告法罚则起点二十万元，一次翻车抵掉全年利润。
   所有对外文案先过 `compliance.py`。
4. **不照搬跨平台打法。** 抖音的内容逻辑与淘宝的搜索逻辑完全不同。
5. **数据以商家后台为准。** 本技能内置的费率、行业基准均为公开参考值，
   实际决策必须用后台真实数据校准。
6. **不编造数据。** 无法确认的数字应明确说明其为估算或参考区间，
   并提示用户核实来源。

## 免责声明

本技能提供的费率、行业基准、退货率区间等均为公开信息与行业经验值，
平台规则持续变化，使用前须以各平台官方最新公示为准。
合规检查结果为风险自查提示，不构成法律意见，
具体认定以市场监管部门为准。
